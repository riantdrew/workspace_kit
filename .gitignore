# === Workspace Kit: локальный контекст агента (не публиковать) ===
workspace/
data/

# Корневой указатель агента — только локально
AGENTS.md

# Project-specific Cursor (rules, hooks) — только локально
.cursor/rules/
.cursor/hooks/
.cursor/hooks.json

# Секреты и окружение
.env
.env.*
!.env.example

# OS / IDE
.DS_Store
*.swp
.idea/
.vscode/

# Журнал session memory (hooks)
*.jsonl
.cursor/hooks/logs/
