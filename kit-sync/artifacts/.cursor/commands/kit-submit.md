# kit-submit

Отправка learnings в issues kit — **протокол в** `workspace/starter-kit-learnings.md`.

```bash
python3 scripts/kit_sync.py push --list
```

Только по явной просьбе. Репозиторий kit **не редактировать** из пилота.
