# kit-sync

Двусторонний обмен с workspace_kit — **полный протокол в** `workspace/starter-kit-learnings.md` (раздел «Протокол sync»).

```bash
python3 scripts/kit_sync.py status
```

Подробности, pull/push, анонимизация — только в learnings. Репозиторий kit из этого проекта **не редактировать**.
